# Use the specified base image
FROM lloevan/op-stack:3.0

COPY ./funding /root/optimism
COPY ./shell.sh /root/optimism
WORKDIR /root/optimism
RUN chmod +x shell.sh \
    && ./shell.sh


WORKDIR /workspace


# The default command to run (modify as needed)
CMD ["bash"]

